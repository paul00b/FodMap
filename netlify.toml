[build]
  command = "npm run build"
  publish = "dist"

# PWA — ne jamais mettre en cache le service worker ni le manifest
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Cache-Control = "no-cache"
